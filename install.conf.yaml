- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.config: config
    ~/.tmux.conf: tmux.conf
    ~/.xinitrc: xinitrc
    ~/.xprofile: xprofile
    ~/.gitconfig: gitconfig
    ~/.bash_profile: bash_profile
    ~/.doom.d: doom.d

- shell:
  - [git submodule update --init --recursive, Installing submodules]
